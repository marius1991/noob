<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:noobapp="http://schemas.android.com/apk/res-auto">


    <!-- Register -->
    <item android:id="@+id/register"
        android:title="Register"
        noobapp:showAsAction="never" />

    <!-- Neue Location anlegen -->
    <item android:id="@+id/location"
        android:title="NeueLocation"
        noobapp:showAsAction="never"/>

    <!-- Logout -->
    <item android:id="@+id/Logout"
        android:title="Logout"
        noobapp:showAsAction="never" />

    <!-- Kontoverwaltung -->
    <item android:id="@+id/Kontoverwaltung"
        android:title="Kontoverwaltung"
        noobapp:showAsAction="never" />
</menu>